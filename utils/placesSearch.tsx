import { GOOGLEMAP_KEY } from '@env';

export default async function placesSearch() {
    const apiKey = GOOGLEMAP_KEY; // Replace with your actual API key

    const placesSearchUrl = `https://places.googleapis.com/v1/places:searchText`;

}
